# Database

Esta pasta contém artefatos de banco de dados para o InsureFlow.

Estrutura:
- `seeds/` - scripts de seed para populações iniciais de dados.
- `backups/` - instruções e exemplos de backup.

Use o Docker Compose em `docker/docker-compose.yml` para iniciar PostgreSQL e Redis.
