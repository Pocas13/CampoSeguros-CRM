"use client";import{useState,type FormEvent}from'react';import{useRouter}from'next/navigation';import{useMutation,useQueryClient}from'@tanstack/react-query';import InsurerForm,{emptyInsurer}from'@/components/insurers/InsurerForm';import{createInsurer}from'@/services/insurers';
export default function Page(){const[v,setV]=useState(emptyInsurer),r=useRouter(),q=useQueryClient();const m=useMutation({mutationFn:createInsurer,onSuccess:async x=>{await q.invalidateQueries({queryKey:['insurers']});r.push(`/insurers/${x.id}`)}});const s=(e:FormEvent<HTMLFormElement>)=>{e.preventDefault();m.mutate(v)};return <div className="mx-auto max-w-5xl"><h1 className="mb-6 text-3xl font-bold">Nova companhia</h1><InsurerForm values={v} onChange={setV} onSubmit={s} onCancel={()=>r.push('/insurers')} busy={m.isPending} error={m.isError?'Não foi possível guardar.':undefined}/></div>}
